# 蟑螂桌宠 · 操作说明

透明置顶桌面宠物。平时躲在屏幕角落，**互动才会出来**；鼠标凑近藏身位置时，会换地方躲。

> **焦点快捷键：** 先**点一下蟑螂**再按键。  
> **全局热键 / 菜单栏·托盘：** 无需焦点（Mac 全局热键需在「系统设置 → 隐私与安全性 → 辅助功能」允许终端或 Python）。

按 **Esc** 退出。启动后终端会打印提示；按 **H** 看气泡帮助。

---

## 产品：设置 / 托盘 / 热键

### 配置文件（工作目录）

| 文件 | 作用 |
|------|------|
| `settings.json` | 气泡开关、打工/财务提醒、系统告警、强制穿透、默认跟随、启用话术包、皮肤、热键 |
| `progress.json` | 亲密度、摸头/喂食/召唤次数、成就称号、已解锁皮肤 |

首次启动会自动生成默认 `settings.json`。

### 菜单栏（Mac）/ 系统托盘（Win）

点击 🪳 图标：召唤、系统总览、切换点击穿透、状态、开关气泡/提醒/告警、换皮肤、会计对喷、故事大会、休息提醒、显示称号、重载话术包、退出。

### 全局热键（默认可在 settings.json 改）

| 组合 | 效果 |
|------|------|
| `Ctrl+Alt+R` | 召唤 |
| `Ctrl+Alt+/` | 系统总览 |
| `Ctrl+Alt+P` | 切换强制穿透（干活模式） |
| `Ctrl+Alt+S` | 状态 |
| `Ctrl+Alt+B` | 会计对喷 |
| `Ctrl+Alt+T` | 故事大会 |
| `Ctrl+Alt+Q` | 退出 |

窗口内也可：`-` 穿透 · `=` 下一皮肤 · `T` 故事大会。

### 点击穿透

- **默认可点互动：** 鼠标在蟑螂上可点，离开后穿透到下层窗口。
- **强制穿透：** 始终点不中蟑螂，方便打字/点网页；用热键或菜单关闭穿透后再点。

---

## 双宠互怼（会计蟑螂）

默认开启功能，但**平时只显示主宠一只**；对喷时「会计蟑螂」才会出现在右侧，结束后淡出。

| 操作 | 效果 |
|------|------|
| `,` 或 `Ctrl+Alt+B` 或菜单「会计对喷」 | 立刻对喷一轮黑话（会计现身） |
| `.` 或菜单「开关会计蟑螂」 | 开启/关闭对喷功能 |
| 自动 | 约 2–5 分钟偶尔对喷（可在 `settings.json` 调 `buddy_banter_min/max`） |

主宠白气泡、同伴奶油金气泡。对喷话题含打工、编程、摸鱼、抬杠等。对喷 5 次解锁称号「对账搭子」。

关闭：`settings.json` 里 `"accountant_buddy": false`。

---

## 故事大会 / 休息提醒 / 周期表演

对照常见桌宠互动（不依赖大模型）：

| 功能 | 操作 | 说明 |
|------|------|------|
| 故事大会 | `T` / `Ctrl+Alt+T` / 菜单 | 本地短篇连播气泡（约 12 组随机） |
| 休息提醒 | 菜单开关；默认约每小时 | 现身屏幕中央催休息；**点一下蟑螂**可关掉当次提示 |
| 周期表演 | 自动（可关） | 约 45–90 秒随机探头/摆拍/跳舞 + 一句闲聊 |

设置项：`rest_reminder`、`rest_reminder_interval_sec`、`idle_showcase`、`idle_showcase_min/max`。  
听完故事 3 次解锁称号「缝里说书人」。

---

目录：`packs/*.json`。内置：`worker`、`finance`、`programmer`、`festival`。

在 `settings.json` 的 `enabled_packs` 里增删 id；或菜单「重载话术包」。改 JSON 即可加黑话，无需改主程序。

---

## 皮肤与成就

| 皮肤 | 说明 |
|------|------|
| `default` | 默认贴图 |
| `gold` | 金色着色 |
| `ghost` | 幽灵着色 |

可把自定义 PNG 放到 `skins/名字.png`。摸头、喂食、召唤、告警会累计进度并解锁称号。

---

## 习性（自动行为）

| 行为 | 说明 |
|------|------|
| 默认躲藏 | 启动问候后钻进角落，半透明 |
| 鼠标靠近 | 躲着时鼠标凑近，会慌张换角落 |
| 打工/财务提醒 | 到点冒泡（可在设置关） |
| 久坐轻推 | 约 25–40 分钟嘀咕一声 |
| 休息提醒 | 约每小时现身催休息（可关） |
| 周期表演 | 约 45–90 秒随机动作+闲聊（可关） |
| 系统告警 | 资源过高吐槽 + 特效表演（可关） |
| 天气 | 启动后尝试拉取，供中键 / `W` |

---

## 鼠标操作

在蟑螂本体上操作。强制穿透开启时点不到，请先关穿透。

### 左键

| 操作 | 效果 |
|------|------|
| 单击**左半边**（头） | 摸头，亲密度累积 |
| 单击**右半边**（尾） | 吓一跳（「咬你哦」等吐槽） |
| 双击 | 疯跑 |
| 三击 | 被戳；偶尔冒一句故事开头 |
| 连摸 5/10/15… | 里程碑气泡 |
| 拖动 / 左右甩 | 拎走；甩会晕 |
| 长按不移（约 0.55s） | 跳舞 |

### 修饰键 + 左键

| 组合 | 效果 |
|------|------|
| **⌥ / Alt** + 点 | 喂食 |
| **⇧ Shift** + 点 | 跳舞 |
| **⌃ Control** + 点 | 大餐 |
| **⌘ / Win** + 点 | 召唤 |

### 右键 / 中键 / 滚轮

| 操作 | 效果 |
|------|------|
| 右键 | 睡觉；再右键钻回角落 |
| 右键拖 | 甩飞 |
| 中键 | 日期 · 时间 · 天气 |
| 滚轮 | 旋转 |

---

## 键盘速查

| 键 | 效果 |
|----|------|
| ← → ↑ ↓ / 空格 | 轻推 / 跳 |
| `F` `P` `A` `R` | 喂 / 戳 / 舞 / 冲刺 |
| `B` `C` `M` `U` `L` `K` | 回家 / 躲 / 跟随 / 翻肚 / 追光 / 召唤 |
| `Q` `E` `Z` `V` `O` `I` `X` `N` | 探头 / 觅食 / 疯跑 / 受惊 / 摆拍 / 闲聊 / 对打 / 躲猫猫 |
| `-` `=` | 穿透 / 下一皮肤 |
| `D` `T` `W` `S` | 日期 / 时间 / 天气 / 状态 |
| `G` `J` `Y` `1`–`4` | 打工提醒 / 黑话 / 对齐 / 站会·复盘·摸鱼·反PUA |
| `5`–`0` `;` | 财务系列 |
| `/` `[` `]` `\` `'` | 监控总览 / CPU / 内存 / 磁盘 / 网络 |
| `H` / `Esc` | 帮助 / 退出 |

---

## 平台差异

| 项目 | macOS | Windows |
|------|-------|---------|
| 入口 | `python3 cockroach_pet.py` | `python` / `run.bat` / exe |
| 托盘 | 菜单栏 🪳 | 系统托盘图标 |
| 依赖 | `requirements-mac.txt` | `requirements.txt`（含 pystray、pillow） |

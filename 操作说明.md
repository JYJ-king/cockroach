# 小猫桌宠 · 操作说明

透明置顶桌面宠物。平时在屏幕角落**趴窝**（始终清晰可见）；鼠标凑近会换窝；互动才会跑出来玩。

> **焦点快捷键：** Mac 先点小猫再按键。**Windows**：鼠标放在小猫上（或刚点过）即可。**默认极简模式**下字母键很少，请优先用菜单。  
> **全局热键 / 菜单栏·托盘：** 无需焦点（Mac 全局热键需在「系统设置 → 隐私与安全性 → 辅助功能」允许终端或 Python）。  
> **Windows：** 托盘图标可能在任务栏隐藏区（点 `^`）；「显示桌面」后小猫会自动回到桌面最前。若点小猫没反应，托盘里确认未开「点击穿透」。

按 **Esc** 退出。启动后终端会打印提示；按 **H** 看气泡帮助。

## 极简模式（默认开启）

职场用户不必背几十个快捷键。默认 **极简模式**：

| 一级菜单 | 说明 |
|----------|------|
| 召唤过来 | 小猫跑到身边 |
| 摸头 | 等同点猫头 |
| 投喂 | 喂小鱼干 |
| 纸箱 | 钻纸箱 |
| 睡觉 | 睡觉 / 再点叫醒 |
| 专注番茄钟 | 开 25 分蹲守；再点取消 |
| 状态 | 心情与亲密度 |

其余猫咪互动、故事、对喷、提醒开关等放在「**更多互动**」「**更多设置**」二级菜单。  
极简下键盘只保留：`N` 纸箱 · `K` 召唤 · `F` 投喂 · `H` 帮助 · 右键睡觉；**⌥/Alt+字母** 专属键关闭。  
菜单点「关闭极简模式」可恢复完整菜单与全部快捷键（`settings.json` → `simple_mode`）。

## 首次引导

新用户（`progress.json` 不存在，或 `onboarding_completed` 为 false）启动约 **2 秒**后出现气泡引导（**非弹窗、不挡干活**）：

0. **破冰**：「要花 20 秒设置一下吗？」→ 好一下 / 直接开始玩  
1. **月结窗口**：月初 1–5 / 月末 25–31 / 月中 10–15 / 不用随缘  
2. **养生节奏**：温和 / 标准 / 严格 / 先别催我  
3. **互动频率**：5 / 10 / 30 / 60 分钟在屏幕上动一次（不选默认 **5 分钟**）  
4. **形象锁定**：就要这只 / 随缘换  

每步是气泡上的小按钮；**约 8 秒不点**会自动跳过该步；步与步之间留 **4–5 秒**呼吸感。全跳过或问完后写入 `onboarding_completed: true`，并冒结束语 + 财务行话彩蛋。菜单「**重新开始引导**」可重置。老用户（已有互动记录）不会被突然弹出。

## 形象与动作

主形象为小猫动画（**无半透明淡化**）。默认每次启动从下列形象中**随机选一只**；可用菜单**锁定当前形象**，下次启动不再随机。

1. 本地经典 GIF：`image/` 下 Idle / Waving / Running / Waiting / Review  
2. Codex 社区猫咪：`codex_pets/`（来自 [awesome-codex-pet](https://github.com/legeling/awesome-codex-pet)，素材多为非商用许可，见该目录 README）

| 操作 | 说明 |
|------|------|
| 菜单「下一个形象」 | 当场切换下一只猫 |
| 菜单「锁定/解锁形象」 | 锁定后启动气泡显示「固定形象:xxx」，下次仍用该只 |
| `=` / 「下一个皮肤」 | 仅切换色调（default / gold / ghost），与形象无关 |

启动气泡会提示「今日形象」或「固定形象」。更新 Codex 猫：`python3 scripts/fetch_codex_cats.py`

| GIF / 动作行 | 何时出现 |
|-----|----------|
| Idle | 回窝静止、默认待机 |
| Running | 走路 / 奔跑 / 跟随 / 跑酷 / 打猎 |
| Waving | 打招呼、开心、跳舞、踩奶、闲聊、对喷 |
| Waiting | 好奇、观鸟、睡觉、面团、露肚 |
| Review | 故事大会、摆拍、舔毛、月结审查 |

### 猫咪专属互动

> 下列 ⌥/Alt 快捷键仅在**关闭极简模式**后生效；极简请用菜单「更多互动」。

| 操作 | 效果 |
|------|------|
| 点**上半身** | 摸头（连摸 3 次触发**踩奶**） |
| 点**下半身** | 逗弄 / 吓跑 |
| 双击 | 跑酷冲刺 |
| `N` | 钻纸箱 |
| `C` | 回窝面团 |
| `E` | 打猎 / 追毛线 |
| `Q` | 观鸟张望 |
| `L` | 追激光笔 |
| `U` | 露肚皮 |
| `Z` / `V` / `X` | 跑酷 / 炸毛 / 扑击 |
| **修饰键 + 字母** | 猫咪专属（见下表；Mac=⌥，Windows=Alt） |
| 右键 | 睡觉 |

### 点击修饰键（操作相同，键位按平台）

| 效果 | macOS | Windows |
|------|-------|---------|
| 召唤过来 | `⌘` + 点击 | `Ctrl+Shift` + 点击 |
| 大餐投喂 | `Ctrl` + 点击 | `Ctrl` + 点击 |
| 普通喂食 | `⌥` + 点击 | `Alt` + 点击 |
| 跳舞 | `Shift` + 点击 | `Shift` + 点击 |

> Windows 不用 `Win+点击` 做召唤：系统常会吞掉 Win 键。也可用 `K` 或全局热键 `Ctrl+Alt+R`。

### 猫咪专属（Mac=`⌥` / Windows=`Alt`）

| 组合 | 效果 |
|------|------|
| `⌥/Alt+M` | 连喵三声 |
| `⌥/Alt+S` | 晒太阳 |
| `⌥/Alt+R` | 抓挠 |
| `⌥/Alt+G` | 叼礼物 |
| `⌥/Alt+T` | 死盯 |
| `⌥/Alt+N` | 推桌 |
| `⌥/Alt+H` | 蹭头 |
| `⌥/Alt+C` | 颤叫观鸟 |
| `⌥/Alt+I` | 傲娇无视 |
| `⌥/Alt+K` / `⌥/Alt+B` | 踩奶 / 舔毛 |
| `⌥/Alt+A` | 随机猫互动 |

菜单栏/托盘也可直接点这些互动。

---

## 产品：设置 / 托盘 / 热键

### 配置文件（工作目录）

| 文件 | 作用 |
|------|------|
| `settings.json` | 气泡开关、打工/财务提醒、系统告警、强制穿透、默认跟随、启用话术包、皮肤、热键 |
| `progress.json` | 亲密度、摸头/喂食/召唤次数、成就称号、已解锁皮肤 |

首次启动会自动生成默认 `settings.json`。

### 菜单栏（Mac）/ 系统托盘（Win）

点击小猫图标：召唤、系统总览、穿透、提醒、会计对喷、故事、休息、鼠标寻访、**猫咪互动**（连喵/晒太阳/抓挠等）、AI、称号、退出。

### 全局热键（默认可在 settings.json 改）

| 组合 | 效果 |
|------|------|
| `Ctrl+Alt+R` | 召唤 |
| `Ctrl+Alt+/` | 系统总览 |
| `Ctrl+Alt+P` | 切换强制穿透（干活模式） |
| `Ctrl+Alt+S` | 状态 |
| `Ctrl+Alt+B` | 会计对喷 |
| `Ctrl+Alt+T` | 故事大会 |
| `Ctrl+Alt+A` | 开关 AI |
| `Ctrl+Alt+Q` | 退出 |

窗口内也可：`-` 穿透 · `=` 下一皮肤 · `T` 故事大会。

### 点击穿透

- **默认可点互动：** 鼠标在小猫上可点，离开后穿透到下层窗口。
- **强制穿透：** 始终点不中小猫，方便打字/点网页；用热键或菜单关闭穿透后再点。

---

## 双宠互怼（会计猫）

默认开启功能，但**平时只显示主宠一只**；对喷时「会计猫」才会出现在右侧，结束后立刻隐去（无淡化）。

| 操作 | 效果 |
|------|------|
| `,` 或 `Ctrl+Alt+B` 或菜单「会计对喷」 | 立刻对喷一轮（会计现身） |
| `.` 或菜单「开关会计猫」 | 开启/关闭对喷功能 |
| 菜单「标记月结中(应援)」 | 强制进入**应援模式**（再点取消） |
| 菜单「这次不是月结(取消应援)」 | 取消当前应援，并**当日抑制**自动触发；反馈写入 `progress.json` 供以后调阈值 |

主宠白气泡、同伴奶油金气泡。对喷 5 次解锁称号「对账搭子」。

### 月结应援（情境感知）

赶上月结/审计，被阴阳会添堵。应援模式下：

- **降频**：自动对喷间隔约拉长 2.6 倍，开口概率降低  
- **改语气**：换成鼓励话术（陪你、喝水、你很棒），不催票不调侃加班  
- **收工仪式**：应援窗口自然结束后，**第一次**回家（`C`/面团）或睡觉（右键/菜单）时，专属庆祝台词 + 伸懒腰动画 + 小礼花（误判点「这次不是月结」不会触发）  
- **触发方式**  
  1. 菜单手动「标记月结中」  
  2. 自动（`buddy_auto_support`，默认开）：每月 **1–5 / 25–月末**，或工作日 **CPU≥75% / 内存≥85%**（高负荷会保持约 15 分钟）
- **误判反馈**：菜单「这次不是月结」→ 立刻关应援，自动应援抑制到当天结束（手动标记仍可开）；事件写入 `progress.support_feedback`（含 reasons / cpu / mem / 日期），计数在 `support_fp_stats`

状态气泡会显示「月结应援中」。可在 `settings.json` 关 `buddy_auto_support` 只保留手动标记。

---

## 故事 / 休息 / 自主行为

| 功能 | 入口 | 说明 |
|------|------|------|
| 故事大会 | `T` / 菜单 / `Ctrl+Alt+T` | 本地或 AI 短篇连播 |
| 休息提醒 | 菜单开关；默认约每小时 | 现身屏幕中央催休息；**点一下小猫**可关掉当次提示；间隔见 `rest_reminder_interval_sec` |
| **养生提醒** | 菜单「开关养生提醒」；默认开 | **护眼 / 喝水 / 伸展**轻量气泡；菜单「切换养生节奏」在温和/标准/严格间切换；也可在 `settings.json` 改秒数 |
| **专注番茄钟** | 极简菜单「专注番茄钟」；再点取消 | 默认 **25 分钟**（`focus_pomodoro_sec`）倒计时；期间猫安静蹲守、压低喧哗；结束后**跑向指针**催休息（点猫关掉）；比固定养生更贴深度工作 |
| 鼠标寻访 | 菜单「开关鼠标寻访」；默认开 | 鼠标约 **30 分钟**不动，小猫跑向指针并随机互动（挥爪/踩奶/跳舞/催休息等） |
| 周期表演 | 自动 | 约 45–90 秒随机动作（含舔毛）+ 闲聊 |
| **自主行为** | 菜单「开关自主行为」；默认开 | 无人操作约 **45 秒**后，按作息自行**散步 / 发呆 / 打瞌睡 / 攀爬 / 倒挂** |
| **会议静默** | 菜单「开关会议/投屏静默」；默认开 | **共享屏幕 / 投屏镜像**时收起；**截图快捷键**时短暂躲闪；开会中压低喧哗；会开完（持续约 45 秒以上）冒一句「辛苦啦,开完了?」 |

### 作息

- **白天**（早/午/下午）：更常散步、攀爬、倒挂  
- **傍晚**：偏发呆、散步，少折腾  
- **深夜**：更常打瞌睡、发呆  
- **专注/勿扰**（尽力检测）：macOS 专注模式、Windows 专注助手/防干扰；若检测到开启，按安静档（偏好发呆/瞌睡）；检测失败则仍按昼夜  
- **会议软件运行中**：安静档，并暂停周期表演/休息催促/对喷等主动喧哗  
- **共享屏幕 / 投屏镜像中**：直接收起桌宠（结束后约 3 秒再现身）；含 macOS AirPlay/镜像、Windows「复制这些显示器」/无线投影（尽力检测）  
- **截图时**：监听 ⌘⇧3/4/5、Win+Shift+S、PrtSc，立刻躲闪约 2.5 秒后恢复（不拦截按键；不因 Snipaste 等常驻工具一直藏着）  

点小猫或按快捷键会打断并推迟下一次自主行为。

听完故事 3 次解锁称号「说书小猫」。

### 养生节奏（默认 standard）

| 预设 | 护眼 | 喝水 | 伸展 |
|------|------|------|------|
| gentle（温和） | 30 分 | 40 分 | 60 分 |
| **standard（标准）** | **20 分** | **30 分** | **45 分** |
| strict（严格） | 15 分 | 20 分 | 30 分 |

自定义：编辑 `settings.json` 的 `care_eye_sec` / `care_water_sec` / `care_stretch_sec`（秒），并把 `care_preset` 设为 `custom`。话术来自 `packs/wellness.json`，可改文案后菜单「重载话术包」。

---

## AI（可选）

见下文与 `secrets.example.json`。失败自动回退本地台词。

---

## 打工 / 财务 / 监控

| 类型 | 说明 |
|------|------|
| 打工/财务提醒 | 到点冒泡（可在设置关） |
| 系统监控 | `/` 总览；CPU/内存高时小猫会表演 |

---

## 鼠标与窗口内快捷键

在小猫本体上操作。强制穿透开启时点不到，请先关穿透。

常用：`H` 帮助 · `N` 纸箱 · `C` 回窝 · `E` 打猎 · `T` 故事 · `-` 穿透 · `=` 皮肤 · `G`/`J` 打工 · `/` 监控总览 · `Esc` 退出。

| 键 | 效果 |
|----|------|
| `G` `J` `Y` `1`–`4` | 打工提醒 / 黑话 / 对齐 / 站会·复盘·摸鱼·反PUA |
| `5`–`0` `;` | 财务话术 / 月结 / 审计 / 报销 / 税务 / 发薪 / 财务提醒 |
| `F` / `A` / `M` / `B` | 喂食 / 跳舞 / 跟随 / 回家 |
| `P` / `R` | 戳 / 跑 |

---

## 平台差异

| | macOS | Windows |
|--|-------|---------|
| 入口 | `python3 cockroach_pet.py` | `python` / `run.bat` / exe |
| 依赖 | `requirements-mac.txt`（含 pillow） | `requirements.txt` |
